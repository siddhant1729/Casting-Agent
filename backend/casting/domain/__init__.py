"""Records and the synthetic catalog.

Knows nothing about ranking, explanation, models, or presentation. Everything
here is a value type or a generator of value types, so any layer above can be
tested against real records rather than fixtures.
"""
